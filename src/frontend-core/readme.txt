This directory contains code that logically belongs to the frontend
but which has to be separated from the frontend tsconfig.json so
that it can be tested with jest via a jest-compatible tsconfig.json.